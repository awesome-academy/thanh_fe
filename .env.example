# Auth.js / NextAuth.js Secret (Generate using `npx auth secret` or `openssl rand -base64 32`)
AUTH_SECRET=your_auth_secret_key_here

# Enable Auth Trust Host for Self-Hosted Production Deployments
AUTH_TRUST_HOST=true
